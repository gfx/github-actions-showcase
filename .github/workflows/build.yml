name: Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: []
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - uses: actions/checkout@v3
      - run: ./tools/build-foo-bar ${{ github.actor }}
